"""Project-derived classifications of source records (EP-6 onward).

Every classification here is **format-based** (docs/CLAIMS.md, C-2): a mapping
from a provider's own typed vocabulary to a small set of store-format classes,
published as a versioned table in the method card, with no nutrition-quality
adjective anywhere, including in identifiers. :mod:`~phillysim.classify.store_format`
is the first: USDA SNAP store types to format classes.
"""
